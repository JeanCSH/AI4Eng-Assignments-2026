Pregunta 3: Pipeline de Clasificación de Imágenes Médicas (Sklearn)
Contexto: Para automatizar el diagnóstico asistido por computadora, es necesario preprocesar los rasgos extraídos de imágenes (como texturas o formas de tumores) antes de entrenar un clasificador.

Misión: Crea una función llamada entrenar_pipeline_diagnostico que reciba una matriz de características X y un vector de etiquetas y. La función debe:

Crear un Pipeline de Scikit-Learn que incluya:

Un escalador StandardScaler.

Un selector de características SelectKBest usando f_classif para elegir las 5 mejores variables.

Un clasificador LogisticRegression con una regularización C=0.1.

Entrenar el pipeline con los datos recibidos y devolver el objeto del pipeline entrenado.